#include <stdio.h>
int main()
{
    int A = 1;
    // Unary Operator
    // A = A++; // post increment +1 //A++ = A+1
    // A = A--; // post decrement -1
    A = --A; // pre increment 1-A
    int B = 10;
    int C = 10;

    if (B == C){
        printf("B Sama dengan C\n");
    }
    else {
        printf("B lebih kecil atau lebih besar  C\n");
    }
    //perbandingan 
    //< > <= >= dll


    
}